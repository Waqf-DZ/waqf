function isEmailValid(email) {
  // @todo: will replace this later
  return email ? true : false
}

function isPhoneValid(phoneNumber) {
  // @todo: will replace this later
  return phoneNumber ? true : false
}

module.exports = {
  isEmailValid,
  isPhoneValid,
}
